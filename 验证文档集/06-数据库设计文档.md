# 数据库设计文档：电商平台 v4.0

> **文档版本**：v4.0.0
> **文档状态**：已评审
> **编写日期**：2026-08-08
> **编写人**：架构组

---

## 一、概念结构设计

### 1.1 ER图（实体-联系图）

ER图用于描述数据库的概念模型，包括实体、属性和实体之间的联系。

**核心实体**：

| 实体 | 说明 | 主要属性 |
|------|------|---------|
| 用户(User) | 平台注册用户 | 用户ID、用户名、密码、手机号、邮箱 |
| 商品(Product) | 平台售卖商品 | 商品ID、名称、价格、库存、分类 |
| 订单(Order) | 用户下单记录 | 订单ID、用户ID、总金额、状态、时间 |
| 订单项(OrderItem) | 订单中的商品明细 | 明细ID、订单ID、商品ID、数量、单价 |
| 支付(Payment) | 订单支付记录 | 支付ID、订单ID、支付方式、金额、状态 |
| 优惠券(Coupon) | 营销优惠券 | 优惠券ID、用户ID、面额、有效期 |
| 评价(Review) | 商品评价 | 评价ID、用户ID、商品ID、评分、内容 |
| 地址(Address) | 用户收货地址 | 地址ID、用户ID、省市区、详细地址 |

**实体关系**：

| 关系 | 源实体 | 目标实体 | 类型 | 说明 |
|------|--------|---------|------|------|
| 下单 | 用户 | 订单 | 1:N | 一个用户可下多个订单 |
| 包含 | 订单 | 订单项 | 1:N | 一个订单包含多个商品 |
| 支付 | 订单 | 支付 | 1:1 | 一个订单对应一次支付 |
| 评价 | 用户 | 评价 | 1:N | 一个用户可写多个评价 |
| 拥有 | 用户 | 优惠券 | 1:N | 一个用户可有多张优惠券 |
| 收货 | 用户 | 地址 | 1:N | 一个用户可有多个地址 |

### 1.2 数据字典

**用户表(user)数据字典**：

| 字段名 | 数据类型 | 长度 | 主键 | 外键 | 非空 | 默认值 | 说明 |
|--------|---------|------|------|------|------|--------|------|
| user_id | BIGINT | 20 | ✅ | — | ✅ | AUTO | 用户唯一标识 |
| username | VARCHAR | 50 | — | — | ✅ | — | 用户名 |
| password | VARCHAR | 100 | — | — | ✅ | — | 密码（BCrypt加密） |
| phone | VARCHAR | 20 | — | — | ✅ | — | 手机号 |
| email | VARCHAR | 100 | — | — | ❌ | — | 邮箱 |
| nickname | VARCHAR | 50 | — | — | ❌ | — | 昵称 |
| avatar | VARCHAR | 255 | — | — | ❌ | — | 头像URL |
| gender | TINYINT | 1 | — | — | ❌ | 0 | 性别(0:未知 1:男 2:女) |
| status | TINYINT | 1 | — | — | ✅ | 1 | 状态(0:禁用 1:正常) |
| user_level | TINYINT | 1 | — | — | ✅ | 1 | 用户等级(1-5) |
| created_at | DATETIME | — | — | — | ✅ | NOW() | 创建时间 |
| updated_at | DATETIME | — | — | — | ✅ | NOW() | 更新时间 |

---

## 二、逻辑设计（关系模型）

### 2.1 用户表(user)

```sql
CREATE TABLE `user` (
  `user_id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码(BCrypt)',
  `phone` VARCHAR(20) NOT NULL COMMENT '手机号',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `gender` TINYINT(1) DEFAULT 0 COMMENT '性别(0:未知 1:男 2:女)',
  `status` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '状态(0:禁用 1:正常)',
  `user_level` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '用户等级',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`),
  UNIQUE KEY `uk_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2.2 商品表(product)

| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| product_id | BIGINT | ✅ | 商品ID |
| category_id | BIGINT | — | 分类ID（外键） |
| name | VARCHAR(200) | — | 商品名称 |
| subtitle | VARCHAR(500) | — | 副标题 |
| main_image | VARCHAR(255) | — | 主图URL |
| detail | TEXT | — | 商品详情(HTML) |
| price | DECIMAL(10,2) | — | 销售价 |
| original_price | DECIMAL(10,2) | — | 原价 |
| stock | INT | — | 库存数量 |
| sales | INT | — | 销量 |
| status | TINYINT | — | 状态(0:下架 1:上架 2:预售) |
| sort_order | INT | — | 排序值 |
| created_at | DATETIME | — | 创建时间 |
| updated_at | DATETIME | — | 更新时间 |

### 2.3 订单表(order)

| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| order_id | VARCHAR(32) | ✅ | 订单ID（分布式ID） |
| user_id | BIGINT | — | 用户ID |
| order_no | VARCHAR(32) | — | 订单编号 |
| total_amount | DECIMAL(12,2) | — | 订单总金额 |
| pay_amount | DECIMAL(12,2) | — | 实付金额 |
| discount_amount | DECIMAL(12,2) | — | 优惠金额 |
| shipping_fee | DECIMAL(10,2) | — | 运费 |
| status | TINYINT | — | 状态(0:待付款 1:已付款 2:已发货 3:已签收 4:已取消 5:退款中 6:已退款) |
| payment_method | VARCHAR(20) | — | 支付方式 |
| payment_time | DATETIME | — | 支付时间 |
| shipping_time | DATETIME | — | 发货时间 |
| receive_time | DATETIME | — | 签收时间 |
| address_id | BIGINT | — | 收货地址ID |
| remark | VARCHAR(500) | — | 订单备注 |
| created_at | DATETIME | — | 创建时间 |
| updated_at | DATETIME | — | 更新时间 |

### 2.4 订单项表(order_item)

| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| item_id | BIGINT | ✅ | 明细ID |
| order_id | VARCHAR(32) | — | 订单ID（外键） |
| product_id | BIGINT | — | 商品ID |
| product_name | VARCHAR(200) | — | 商品名称（快照） |
| product_image | VARCHAR(255) | — | 商品图片（快照） |
| quantity | INT | — | 购买数量 |
| unit_price | DECIMAL(10,2) | — | 单价（快照） |
| total_price | DECIMAL(12,2) | — | 小计金额 |
| created_at | DATETIME | — | 创建时间 |

### 2.5 支付记录表(payment)

| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| payment_id | VARCHAR(32) | ✅ | 支付ID |
| order_id | VARCHAR(32) | — | 订单ID |
| user_id | BIGINT | — | 用户ID |
| payment_method | VARCHAR(20) | — | 支付方式(alipay/wechat) |
| payment_no | VARCHAR(64) | — | 第三方支付流水号 |
| amount | DECIMAL(12,2) | — | 支付金额 |
| status | TINYINT | — | 状态(0:待支付 1:支付中 2:成功 3:失败 4:已退款) |
| callback_time | DATETIME | — | 回调时间 |
| created_at | DATETIME | — | 创建时间 |
| updated_at | DATETIME | — | 更新时间 |

---

## 三、物理设计

### 3.1 存储引擎选择

| 表名 | 存储引擎 | 说明 |
|------|---------|------|
| user | InnoDB | 支持事务、行级锁 |
| product | InnoDB | 支持事务 |
| order | InnoDB | 支持事务 |
| order_item | InnoDB | 支持事务 |
| payment | InnoDB | 支持事务 |
| review | InnoDB | 支持事务 |
| coupon | InnoDB | 支持事务 |

### 3.2 索引设计

**用户表索引**：

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | user_id | 主键 |
| uk_username | 唯一索引 | username | 用户名唯一 |
| uk_phone | 唯一索引 | phone | 手机号唯一 |
| uk_email | 唯一索引 | email | 邮箱唯一 |
| idx_status | 普通索引 | status | 按状态查询 |
| idx_created_at | 普通索引 | created_at | 按时间查询 |

**订单表索引**：

| 索引名 | 索引类型 | 字段 | 说明 |
|--------|---------|------|------|
| PRIMARY | 主键索引 | order_id | 主键 |
| uk_order_no | 唯一索引 | order_no | 订单号唯一 |
| idx_user_id | 普通索引 | user_id | 按用户查询订单 |
| idx_status | 普通索引 | status | 按状态查询 |
| idx_created_at | 普通索引 | created_at | 按时间查询 |
| idx_user_status | 联合索引 | user_id, status | 按用户+状态查询 |

### 3.3 分库分表策略

| 表名 | 分表键 | 分表策略 | 分表数 | 说明 |
|------|--------|---------|--------|------|
| order | user_id | 取模 | 16 | 按用户ID分表 |
| order_item | order_id | 取模 | 16 | 跟随订单分表 |
| payment | order_id | 取模 | 16 | 跟随订单分表 |
| review | product_id | 取模 | 8 | 按商品ID分表 |
| user | user_id | 范围 | 4 | 按ID范围分表 |

### 3.4 分区设计

对于订单表、评论表等大数据量表，可按时间进行水平分区：

```sql
-- 订单表按月分区
ALTER TABLE `order` PARTITION BY RANGE (TO_DAYS(created_at)) (
  PARTITION p202601 VALUES LESS THAN (TO_DAYS('2026-02-01')),
  PARTITION p202602 VALUES LESS THAN (TO_DAYS('2026-03-01')),
  PARTITION p202603 VALUES LESS THAN (TO_DAYS('2026-04-01')),
  PARTITION p202604 VALUES LESS THAN (TO_DAYS('2026-05-01')),
  PARTITION p202605 VALUES LESS THAN (TO_DAYS('2026-06-01')),
  PARTITION p202606 VALUES LESS THAN (TO_DAYS('2026-07-01')),
  PARTITION p202607 VALUES LESS THAN (TO_DAYS('2026-08-01')),
  PARTITION p202608 VALUES LESS THAN (TO_DAYS('2026-09-01')),
  PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

---

## 四、数据安全

### 4.1 敏感数据加密

| 字段 | 加密方式 | 说明 |
|------|---------|------|
| password | BCrypt(cost=12) | 不可逆加密 |
| phone | AES-256-ECB | 可逆加密，密钥统一管理 |
| email | AES-256-ECB | 可逆加密 |
| id_card | AES-256-ECB | 可逆加密 |
| bank_card | AES-256-ECB | 可逆加密 |

### 4.2 数据备份策略

| 备份类型 | 频率 | 保留时间 | 存储位置 |
|---------|------|---------|---------|
| 全量备份 | 每日凌晨2点 | 30天 | 本地+异地 |
| 增量备份 | 每小时 | 7天 | 本地 |
| Binlog备份 | 实时 | 7天 | 本地+异地 |
| 逻辑备份 | 每周 | 90天 | 异地 |

---

## 五、性能优化

### 5.1 慢查询优化

| 优化类型 | 方法 | 示例 |
|---------|------|------|
| 索引优化 | 添加缺失索引 | 为order.user_id添加索引 |
| SQL重写 | 避免全表扫描 | 用JOIN替代子查询 |
| 分页优化 | 使用游标分页 | WHERE id > last_id LIMIT 20 |
| 读写分离 | 读走从库 | 写主库，读从库 |
| 缓存优化 | 热数据缓存 | Redis缓存商品信息 |

### 5.2 连接池配置

| 参数 | 值 | 说明 |
|------|-----|------|
| initialSize | 5 | 初始连接数 |
| minIdle | 10 | 最小空闲连接 |
| maxActive | 50 | 最大活跃连接 |
| maxWait | 3000ms | 获取连接超时时间 |
| timeBetweenEvictionRunsMillis | 60000ms | 空闲连接检查间隔 |
| minEvictableIdleTimeMillis | 300000ms | 连接最小空闲时间 |

---

## 六、附录

### 附录A：建库脚本

```sql
CREATE DATABASE IF NOT EXISTS `ecommerce` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE `ecommerce`;

-- 用户表
CREATE TABLE `user` (
  `user_id` BIGINT(20) NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(50) NOT NULL,
  `password` VARCHAR(100) NOT NULL,
  `phone` VARCHAR(20) NOT NULL,
  `email` VARCHAR(100) DEFAULT NULL,
  `status` TINYINT(1) NOT NULL DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 商品表
CREATE TABLE `product` (
  `product_id` BIGINT(20) NOT NULL AUTO_INCREMENT,
  `category_id` BIGINT(20) NOT NULL,
  `name` VARCHAR(200) NOT NULL,
  `price` DECIMAL(10,2) NOT NULL,
  `stock` INT NOT NULL DEFAULT 0,
  `status` TINYINT(1) NOT NULL DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`product_id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

---

*本文档为数据库设计文档，仅供项目团队内部使用。*
